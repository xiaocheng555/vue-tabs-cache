{
  "name": "vue-tabs-cache",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd vue2 && npm run build && cd ../vue3 && npm run build",
    "deploy": "node script/deploy",
    "publish": "npm run build && node script/deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "^10.1.0",
    "gh-pages": "^4.0.0"
  }
}
